package crypto

// DecryptJWTToken decrypts the given JWT token using server-side encryption
// func DecryptJWTToken(token string) (decryptedToken string, err error) {
// 	// Convert the token string to bytes
// 	tokenBytes := []byte(token)

// 	// Decrypt the token using server-side encryption
// 	decryptedBytes, err := ServerDecrypt(tokenBytes)
// 	if err != nil {
// 		return "", err
// 	}

// 	// Convert the decrypted bytes back to a string
// 	decryptedToken = string(decryptedBytes)

// 	return decryptedToken, nil
// }
